---
name: dotrepos sync

on:
  pull_request:
    types:
      - labeled

jobs:
  sync:
    if: github.event.label.name == 'dotrepos-sync'
    uses: vrozaksen/dotrepos/.forgejo/workflows/dotrepos-sync.yml@main
    secrets: inherit
